[build-system]
requires = ["setuptools>=69.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "prepbettr-backend"
version = "0.1.0"
description = "PrepBettr FastAPI backend"
authors = [{name = "Dikshant Vashistha", email = "dikshant@prepbettr.com"}]
dependencies = []  # Optional; use requirements.txt instead
requires-python = ">=3.13"
readme = "README.md"
license = {text = "MIT"}

[project.optional-dependencies]
dev = []  # Optional reference; main deps in requirements.txt
